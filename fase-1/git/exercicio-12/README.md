# Git 12 - Stash

## Goal

Practice temporarily saving unfinished local changes.

## Commands

git stash
git stash list
git stash pop

## What I learned

I learned that git stash temporarily saves uncommitted changes when I need to change context without creating an incomplete commit.

git stash pop restores the saved changes and removes them from the stash.
