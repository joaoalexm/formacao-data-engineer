# Git 13 - Tags

## Goal

Practice marking important commits with Git tags.

## Commands

git tag
git tag -a <tag> -m "<message>"
git show <tag>
git push origin <tag>

## What I learned

A Git tag gives a permanent name to a specific commit and is commonly used to identify software versions and releases.
